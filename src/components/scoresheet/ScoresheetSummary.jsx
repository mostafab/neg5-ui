import React from "react";

import Card from "@components/common/cards";

const ScoresheetSummary = ({ cycles, teams }) => {
  return <Card title="Summary"></Card>;
};

export default ScoresheetSummary;
